openapi: 3.0.1
info:
  title: guesto
  version: "0.0"
paths:
  /guesto:
    get:
      operationId: index
      responses:
        "200":
          description: index 200 response
          content:
            text/plain:
              schema:
                type: string
